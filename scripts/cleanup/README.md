# Cleanup scripts

These scripts remove the example frontend and template files from the project
and restore a minimal starter structure.

## Usage

Linux / macOS

./scripts/cleanup/clean-example-files.sh

Windows PowerShell

.\scripts\cleanup\clean-example-files.ps1

The scripts will:

- remove example assets and templates
- restore minimal files from `default-files/`