name: Run test migrations
description: Run Doctrine migrations in test environment if they exist

runs:
  using: composite
  steps:
    - name: Run migrations if any exist
      shell: bash
      run: |
        if find migrations -maxdepth 1 -type f -name 'Version*.php' | grep -q .; then
          php bin/console doctrine:migrations:migrate --no-interaction --env=test
        else
          echo "No Doctrine migrations found. Skipping migration step."
        fi