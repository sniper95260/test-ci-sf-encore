name: Lint Symfony container
description: Validate Symfony dependency injection container

runs:
  using: composite
  steps:
    - name: Lint Symfony container
      shell: bash
      run: php bin/console lint:container